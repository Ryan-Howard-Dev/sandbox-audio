<?xml version="1.0" encoding="utf-8" ?>
<!--
  Google Cast, merged into the gplay flavour only.

  The Cast framework finds its configuration through this meta-data, and it names a class that
  exists only in this flavour. Leaving it in the main manifest would make the foss build declare a
  CastOptionsProvider it does not contain, which the manifest merger accepts and the device then
  fails on at runtime.
-->
<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <application>
        <meta-data
            android:name="com.google.android.gms.cast.framework.OPTIONS_PROVIDER_CLASS_NAME"
            android:value="rd.sheepskin.sandboxmusic.CastOptionsProvider"
        />
    </application>
</manifest>
